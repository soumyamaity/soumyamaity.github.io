# section information
section:
  name: Recent Posts
  id: recent-posts
  enable: true
  weight: 2
  showOnNavbar: true
  numShow: 6
  # Can optionally hide the title in sections
  # hideTitle: true

# no additional configuration is required
